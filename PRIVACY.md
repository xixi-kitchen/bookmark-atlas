# Bookmark Atlas 隐私说明

Bookmark Atlas 是本地优先的 Chrome 扩展。

## 处理的数据

- 扩展使用 Chrome Bookmarks API 在本地读取和修改用户书签。
- 扩展使用 Chrome Favicon API 从浏览器本地缓存显示书签网站图标。
- Excalidraw 完整画布和用户添加的附件保存在扩展自己的本地 IndexedDB；不可用时回退到 `chrome.storage.local`。
- 图形、文字、书签引用、视口和素材库会在配额允许时经压缩分块写入 `chrome.storage.sync`；图片和二进制附件不会上传到 Chrome Sync，只保存在当前设备。
- 主题、视图、卡片尺寸和搜索引擎设置保存在 `chrome.storage.sync`；是否跨设备同步由用户的 Chrome 同步设置决定。
- 搜索词不会被扩展记录。只有在用户提交搜索时，搜索词才会发送给用户当前选择的搜索引擎。
- 用户主动选择百度、必应或 Google 后，输入关键词会在输入过程中发送给对应搜索引擎的固定联想接口；使用“Chrome 默认”或自定义引擎时不会调用额外的远程联想接口。扩展不会保存这些联想请求。
- 扩展会在本地读取已打开标签页、浏览历史和书签，用于生成预搜索结果；这些本地结果不会上传到开发者服务器或搜索引擎。
- 用户主动嵌入网页时，该网页会在 iframe 中直接连接其所属网站；网站可能按照浏览器规则接收网络请求、Cookie 等信息。Bookmark Atlas 不代理或记录这些请求。
- 对 Bookmark Atlas 自己发起的 YouTube 官方 `/embed/` 播放器请求，扩展会通过限定扩展 ID 的声明式规则设置固定的 `https://bookmark-atlas.invalid/` Referer 作为客户端标识；规则不读取请求内容，也不作用于其他网站创建的 YouTube 播放器。

## 不会进行的行为

- 不会将书签上传到开发者服务器。
- 不会出售、共享或分析用户书签与搜索记录。
- 不使用广告、追踪器或远程执行代码。
- 仅请求生成预搜索所需的浏览历史与标签页元数据权限；不请求或读取网页正文。
- 不读取网页正文，也不会把标签页、历史记录或书签上传到开发者服务器。

## 删除数据

卸载扩展会删除扩展的本地存储。Chrome 账号同步数据的清理由 Chrome 自身管理。卸载扩展不会删除用户的 Chrome 书签。
